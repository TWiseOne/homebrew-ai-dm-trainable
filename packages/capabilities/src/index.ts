import type {GameState} from "../../domain/src/index.js"; import type {CampaignEngine} from "../../campaign-engine/src/index.js";
export type CapabilityName="inspect_game"|"inspect_actor"|"request_check"|"request_saving_throw"|"start_encounter"|"request_attack"|"end_turn";
export class CapabilityDispatcher{constructor(readonly engine:CampaignEngine,readonly state:GameState){} dispatch(name:CapabilityName,args:any){switch(name){case"inspect_game":return {gameId:this.state.id,sceneId:this.state.currentSceneId,difficulty:this.state.difficulty,actors:this.state.actors.map(a=>({id:a.id,name:a.name,kind:a.kind})),encounter:this.state.encounter};case"inspect_actor":{const a=this.state.actors.find(x=>x.id===args.actorId);if(!a)throw new Error("Unknown actor");return a}case"request_check":return this.engine.check(this.state,args.actorId,args);case"request_saving_throw":return this.engine.save(this.state,args.actorId,args);case"start_encounter":return this.engine.startEncounter(this.state,args.encounterId,args.actorIds);case"request_attack":return this.engine.attack(this.state,args.attackerId,args.targetId,args.actionId);case"end_turn":return this.engine.endTurn(this.state);default:throw new Error(`Unknown capability ${String(name)}`)}}}
