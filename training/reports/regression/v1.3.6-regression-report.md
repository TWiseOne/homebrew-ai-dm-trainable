# V1.3.6 Regression Report

Date: 2026-09-22

## Result

Training-pipeline regression: PASS.

- Python compileall across training scripts/tests: PASS.
- 10/10 V1.3.6 unit regressions: PASS.
- Project-native seed generation: PASS (330 seeds; 11 primary themes).
- Synthetic end-to-end 2,000-record curriculum/envelope exercise using the observed V1.3.5 source skew: PASS after correcting the test expectation to allow deterministic fill across SRD/project-native sources. External `dnd_dm_v3` contribution was capped at 296 records; unresolved_result was capped at 120; total curriculum reached 2,000.
- Application source integrity vs V1.3.5 (`apps/`, `packages/`, `tests/`, `tsconfig.json`): PASS, 22/22 files byte-identical.
- `npm test`: attempted, but this isolated build environment lacks installed `@types/node`; `npm install` could not complete within the environment timeout. No application source changed in V1.3.6, so this is an environment/dependency limitation rather than a detected application regression. Run `npm install && npm test` on the Rocky host as the final host-side confirmation.

## Regression cases

1. Rules lookups stay informational.
2. Explicit in-fiction current actions may classify mechanical.
3. System/tool-schema text is excluded from behavior classification.
4. External behavioral theme caps cannot be exceeded.
5. The observed V1.3.5 skew cannot force 400 external records.
6. Project-native source covers broad DM behavior.
7. Three-source calibration sampling remains stratified.
8. Human-owned roll lifecycle is present in teacher policy.
9. Foreign runtime recreation is forbidden.
10. AI-controlled actor intent is separated from engine mechanical resolution.

## Confidence

88% confidence that V1.3.6 is the final **pre-training pipeline build**. The remaining uncertainty is empirical teacher-output quality, not a known architecture defect. The 50-record calibration and human review remain the release gate before 500/2,000 generation or training.
